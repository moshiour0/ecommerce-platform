# websocket-gateway — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: websocket-gateway
  namespace: ecommerce
  labels:
    app: websocket-gateway
    tier: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: websocket-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: websocket-gateway
        tier: service
    spec:
      containers:
      - name: websocket-gateway
        image: ecommerce-platform-websocket-gateway:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: NODE_OPTIONS
          value: --require node-common/tracing
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: websocket-gateway
        - name: PORT
          value: '8003'
        - name: REDIS_URL
          value: redis://redis:6379
        resources:
          requests:
            cpu: 100m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 768Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        ports:
        - containerPort: 8003
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: websocket-gateway
  namespace: ecommerce
  labels:
    app: websocket-gateway
spec:
  type: ClusterIP
  selector:
    app: websocket-gateway
  ports:
  - name: http
    port: 8003
    targetPort: 8003
    protocol: TCP
