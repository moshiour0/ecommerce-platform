# cart-service — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: ecommerce
  labels:
    app: cart-service
    tier: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: cart-service
        tier: service
    spec:
      initContainers:
      - name: schema-init
        image: ecommerce-platform-cart-service:latest
        imagePullPolicy: IfNotPresent
        command:
        - python
        - -c
        - "import asyncio\nfrom app.database import engine, Base\nimport app.models\nasync def m():\n\
          \    async with engine.begin() as c:\n        await c.run_sync(Base.metadata.create_all)\nasyncio.run(m())\n\
          print('schema ready')\n"
        env: &id001
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecommerce-secrets
              key: POSTGRES_PASSWORD
        - name: DATABASE_URL
          value: postgresql+asyncpg://admin:$(POSTGRES_PASSWORD)@postgres:5432/cart_db
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        securityContext: &id002
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
      containers:
      - name: cart-service
        image: ecommerce-platform-cart-service:latest
        imagePullPolicy: IfNotPresent
        env: *id001
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext: *id002
        command:
        - uvicorn
        - app.main:app
        - --host
        - 0.0.0.0
        - --port
        - '8007'
        ports:
        - containerPort: 8007
          name: http
        readinessProbe:
          httpGet: &id003
            path: /health
            port: 8007
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id003
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: ecommerce
  labels:
    app: cart-service
spec:
  type: ClusterIP
  selector:
    app: cart-service
  ports:
  - name: http
    port: 8007
    targetPort: 8007
    protocol: TCP
