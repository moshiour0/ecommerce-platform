# bff-shop — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-shop
  namespace: ecommerce
  labels:
    app: bff-shop
    tier: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bff-shop
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: bff-shop
        tier: service
    spec:
      containers:
      - name: bff-shop
        image: ecommerce-platform-bff-shop:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: CART_SERVICE_URL
          value: http://cart-service:8007
        - name: CATALOG_SERVICE_URL
          value: http://catalog-service:8005
        - name: NODE_OPTIONS
          value: --require node-common/tracing
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: bff-shop
        - name: PORT
          value: '8001'
        - name: SEARCH_SERVICE_URL
          value: http://search-service:8006
        resources:
          requests:
            cpu: 100m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 768Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        ports:
        - containerPort: 8001
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: bff-shop
  namespace: ecommerce
  labels:
    app: bff-shop
spec:
  type: ClusterIP
  selector:
    app: bff-shop
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    protocol: TCP
