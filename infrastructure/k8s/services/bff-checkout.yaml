# bff-checkout — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-checkout
  namespace: ecommerce
  labels:
    app: bff-checkout
    tier: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bff-checkout
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: bff-checkout
        tier: service
    spec:
      containers:
      - name: bff-checkout
        image: ecommerce-platform-bff-checkout:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: CART_SERVICE_URL
          value: http://cart-service:8007
        - name: CATALOG_SERVICE_URL
          value: http://catalog-service:8005
        - name: DELIVERY_QUOTE_SERVICE_URL
          value: http://delivery-quote-service:8011
        - name: FRAUD_SERVICE_URL
          value: http://fraud-service:8014
        - name: NODE_OPTIONS
          value: --require node-common/tracing
        - name: ORDER_SAGA_URL
          value: http://order-saga:8012
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: bff-checkout
        - name: PORT
          value: '8002'
        - name: PRICING_SERVICE_URL
          value: http://pricing-service:8008
        resources:
          requests:
            cpu: 100m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 768Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        ports:
        - containerPort: 8002
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8002
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: bff-checkout
  namespace: ecommerce
  labels:
    app: bff-checkout
spec:
  type: ClusterIP
  selector:
    app: bff-checkout
  ports:
  - name: http
    port: 8002
    targetPort: 8002
    protocol: TCP
