# webhook-handler — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-handler
  namespace: ecommerce
  labels:
    app: webhook-handler
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webhook-handler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: webhook-handler
        tier: worker
    spec:
      containers:
      - name: webhook-handler
        image: ecommerce-platform-webhook-handler:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecommerce-secrets
              key: POSTGRES_PASSWORD
        - name: PSP_WEBHOOK_SECRET
          valueFrom:
            secretKeyRef:
              name: ecommerce-secrets
              key: PSP_WEBHOOK_SECRET
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: webhook-handler
        - name: PAYMENT_DATABASE_URL
          value: postgresql+asyncpg://admin:$(POSTGRES_PASSWORD)@postgres:5432/payment_ledger_db
        - name: PYTHONPATH
          value: /app:/app/shared_libs
        - name: REDIS_URL
          value: redis://redis:6379
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        command:
        - uvicorn
        - app.main:app
        - --host
        - 0.0.0.0
        - --port
        - '8035'
        ports:
        - containerPort: 8035
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8035
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: webhook-handler
  namespace: ecommerce
  labels:
    app: webhook-handler
spec:
  type: ClusterIP
  selector:
    app: webhook-handler
  ports:
  - name: http
    port: 8035
    targetPort: 8035
    protocol: TCP
