# dlq-reprocessor — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dlq-reprocessor
  namespace: ecommerce
  labels:
    app: dlq-reprocessor
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dlq-reprocessor
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: dlq-reprocessor
        tier: worker
    spec:
      containers:
      - name: dlq-reprocessor
        image: ecommerce-platform-dlq-reprocessor:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: AUDIT_URL
          value: http://audit-service:8019/audit
        - name: DLQ_BACKOFF_SECONDS
          value: '3600'
        - name: KAFKA_BROKER
          value: kafka:29092
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: dlq-reprocessor
        - name: PYTHONPATH
          value: /app:/app/shared_libs
        - name: REDIS_URL
          value: redis://redis:6379
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        command:
        - uvicorn
        - app.main:app
        - --host
        - 0.0.0.0
        - --port
        - '8032'
        ports:
        - containerPort: 8032
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8032
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: dlq-reprocessor
  namespace: ecommerce
  labels:
    app: dlq-reprocessor
spec:
  type: ClusterIP
  selector:
    app: dlq-reprocessor
  ports:
  - name: http
    port: 8032
    targetPort: 8032
    protocol: TCP
