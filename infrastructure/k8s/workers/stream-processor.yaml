# stream-processor — Deployment
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stream-processor
  namespace: ecommerce
  labels:
    app: stream-processor
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stream-processor
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: stream-processor
        tier: worker
    spec:
      containers:
      - name: stream-processor
        image: ecommerce-platform-stream-processor:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: PYTHONPATH
          value: /app:/app/shared_libs
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        command:
        - python
        - -m
        - app.main
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30
