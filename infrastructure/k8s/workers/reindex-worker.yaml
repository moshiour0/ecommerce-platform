# reindex-worker — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reindex-worker
  namespace: ecommerce
  labels:
    app: reindex-worker
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reindex-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: reindex-worker
        tier: worker
    spec:
      containers:
      - name: reindex-worker
        image: ecommerce-platform-reindex-worker:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecommerce-secrets
              key: POSTGRES_PASSWORD
        - name: CATALOG_DATABASE_URL
          value: postgresql://admin:$(POSTGRES_PASSWORD)@postgres:5432/catalog_db
        - name: ELASTICSEARCH_URL
          value: http://elasticsearch:9200
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: reindex-worker
        - name: PYTHONPATH
          value: /app:/app/shared_libs
        - name: REINDEX_INTERVAL_SECONDS
          value: '0'
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        command:
        - uvicorn
        - app.main:app
        - --host
        - 0.0.0.0
        - --port
        - '8033'
        ports:
        - containerPort: 8033
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8033
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: reindex-worker
  namespace: ecommerce
  labels:
    app: reindex-worker
spec:
  type: ClusterIP
  selector:
    app: reindex-worker
  ports:
  - name: http
    port: 8033
    targetPort: 8033
    protocol: TCP
