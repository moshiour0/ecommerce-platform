# notification-worker — Deployment + Service
# GENERATED by scripts/generate_k8s.py — do not edit by hand.
# Regenerate after changing docker-compose.apps.yml.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-worker
  namespace: ecommerce
  labels:
    app: notification-worker
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: notification-worker
        tier: worker
    spec:
      containers:
      - name: notification-worker
        image: ecommerce-platform-notification-worker:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecommerce-secrets
              key: POSTGRES_PASSWORD
        - name: NOTIFICATION_DATABASE_URL
          value: postgresql://admin:$(POSTGRES_PASSWORD)@postgres:5432/notification_db
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://jaeger:4318
        - name: OTEL_SERVICE_NAME
          value: notification-worker
        - name: PYTHONPATH
          value: /app:/app/shared_libs
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        command:
        - uvicorn
        - app.main:app
        - --host
        - 0.0.0.0
        - --port
        - '8034'
        ports:
        - containerPort: 8034
          name: http
        readinessProbe:
          httpGet: &id001
            path: /health
            port: 8034
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet: *id001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
      securityContext:
        fsGroup: 1000
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: notification-worker
  namespace: ecommerce
  labels:
    app: notification-worker
spec:
  type: ClusterIP
  selector:
    app: notification-worker
  ports:
  - name: http
    port: 8034
    targetPort: 8034
    protocol: TCP
