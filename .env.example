# ==========================================
# ANTI-GRAVITY PLATFORM CONFIGURATION
# Rule 2.2: Strict Port Mappings (8001-8020)
# ==========================================

# EDGE & BFF (Node.js)
PORT_API_GATEWAY=8000
PORT_BFF_SHOP=8001
PORT_BFF_CHECKOUT=8002
PORT_WS_GATEWAY=8003

# CORE SERVICES (Python FastAPI)
PORT_USER_SVC=8004
PORT_CATALOG_SVC=8005
PORT_SEARCH_SVC=8006
PORT_CART_SVC=8007
PORT_PRICING_SVC=8008
PORT_PROMO_SVC=8009
PORT_TAX_SVC=8010
PORT_DELIVERY_QUOTE_SVC=8011
PORT_ORDER_SAGA=8012
PORT_INVENTORY_SVC=8013
PORT_FRAUD_SVC=8014
PORT_PAYMENT_SVC=8015
PORT_FULFILLMENT_SVC=8016
PORT_NOTIFICATION_SVC=8017
PORT_MEDIA_SVC=8018
PORT_AUDIT_SVC=8019

# INFRASTRUCTURE DATABASES
POSTGRES_USER=admin
POSTGRES_PASSWORD=supersecret
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

REDIS_HOST=localhost
REDIS_PORT=6379

ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

KAFKA_BROKER=localhost:9092
SCHEMA_REGISTRY_URL=http://localhost:8081

# SECURITY (Rule 8: Vault-managed in production; no defaults in source)
# Generate a real value per environment, e.g.:  openssl rand -hex 32
JWT_SECRET=replace-me-with-a-generated-secret
